import { getInput } from "../util";

export async function main1(data: string) {
  // const data = await getInput(__dirname);
  // console.log(data);
  // const input = data.split("\n");
  // return 0;
}

export async function main2(data: string) {
  // const data = await getInput(5, file);
  // const input = data.split("\n");
  // return 0;
}
